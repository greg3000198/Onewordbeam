<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Abbonati a OneWordBeam Premium</title>
    <script src="https://js.stripe.com/v3/"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }

        .container {
            max-width: 900px;
            width: 100%;
        }

        h1 {
            color: white;
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        .subtitle {
            color: rgba(255, 255, 255, 0.9);
            text-align: center;
            font-size: 1.1rem;
            margin-bottom: 3rem;
        }

        .plans {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .plan {
            background: white;
            border-radius: 20px;
            padding: 2.5rem;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
            position: relative;
        }

        .plan:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.3);
        }

        .plan.popular {
            border: 3px solid #667eea;
        }

        .popular-badge {
            position: absolute;
            top: -15px;
            right: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .plan-name {
            font-size: 1.8rem;
            font-weight: 700;
            color: #333;
            margin-bottom: 1rem;
        }

        .plan-price {
            font-size: 3rem;
            font-weight: 700;
            color: #667eea;
            margin-bottom: 0.5rem;
        }

        .plan-price span {
            font-size: 1.2rem;
            color: #666;
        }

        .plan-savings {
            color: #4caf50;
            font-weight: 600;
            margin-bottom: 2rem;
            font-size: 0.95rem;
        }

        .plan-features {
            list-style: none;
            margin-bottom: 2rem;
        }

        .plan-features li {
            padding: 0.8rem 0;
            border-bottom: 1px solid #f0f0f0;
            color: #666;
            font-size: 0.95rem;
        }

        .plan-features li:last-child {
            border-bottom: none;
        }

        .plan-features li::before {
            content: "✓";
            color: #4caf50;
            font-weight: bold;
            margin-right: 10px;
        }

        .subscribe-btn {
            width: 100%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 1.2rem;
            border-radius: 30px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: opacity 0.3s ease;
        }

        .subscribe-btn:hover {
            opacity: 0.9;
        }

        .subscribe-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .back-link {
            display: block;
            text-align: center;
            color: white;
            margin-top: 2rem;
            text-decoration: none;
            font-size: 0.95rem;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        .loading {
            display: none;
            text-align: center;
            color: white;
            margin-top: 2rem;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2rem;
            }
            .plans {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>✨ Sblocca Premium</h1>
        <p class="subtitle">Scegli il piano perfetto per te</p>

        <div class="plans">
            <!-- Piano Mensile -->
            <div class="plan" id="plan-monthly" data-price-id="price_1SRu84RjBqB63SDCMkXeoppT">
                <div class="plan-name">Mensile</div>
                <div class="plan-price">1€<span>/mese</span></div>
                <ul class="plan-features">
                    <li>☀️ Parola mattutina (8:00)</li>
                    <li>🌙 Parola serale (21:00)</li>
                    <li>💡 Consigli personalizzati</li>
                    <li>📊 Statistiche avanzate</li>
                    <li>🎨 Temi personalizzati</li>
                    <li>❌ Cancella quando vuoi</li>
                </ul>
                <button class="subscribe-btn" onclick="subscribe('price_1SRu84RjBqB63SDCMkXeoppT')">
                    Inizia ora
                </button>
            </div>

            <!-- Piano Annuale (Popolare) -->
            <div class="plan popular" id="plan-yearly" data-price-id="price_1SRvCBRjBqB63SDCbJcm3cm6">
                <div class="popular-badge">💎 Più Popolare</div>
                <div class="plan-name">Annuale</div>
                <div class="plan-price">15€<span>/anno</span></div>
                <div class="plan-savings">🎉 Risparmi 20% (3 mesi gratis!)</div>
                <ul class="plan-features">
                    <li>☀️ Parola mattutina (8:00)</li>
                    <li>🌙 Parola serale (21:00)</li>
                    <li>💡 Consigli personalizzati</li>
                    <li>📊 Statistiche avanzate</li>
                    <li>🎨 Temi personalizzati</li>
                    <li>💰 Massimo risparmio</li>
                </ul>
                <button class="subscribe-btn" onclick="subscribe('price_1SRvCBRjBqB63SDCbJcm3cm6')">
                    Risparmia ora
                </button>
            </div>
        </div>

        <a href="/" class="back-link">← Torna alla home</a>

        <div class="loading" id="loading">
            <p>Reindirizzamento a Stripe... ⏳</p>
        </div>
    </div>

    <script>
        // Inizializza Stripe con la chiave pubblicabile
        const stripe = Stripe('pk_live_51SRuJLRjBqB63SDCAyP0s8If3gPVz8esdkvciJSUJbtmlFivVvdIUk0qC6Jp0phgLGhJRFc0AtawvXxjo071XffI000MK1MEFn');

        async function subscribe(priceId) {
            // Disabilita i pulsanti
            const buttons = document.querySelectorAll('.subscribe-btn');
            buttons.forEach(btn => btn.disabled = true);

            // Mostra loading
            document.getElementById('loading').style.display = 'block';

            try {
                // Chiama l'API per creare la sessione di checkout
                const response = await fetch('/api/create-checkout-session', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ priceId }),
                });

                const { sessionId } = await response.json();

                // Reindirizza a Stripe Checkout
                const { error } = await stripe.redirectToCheckout({ sessionId });

                if (error) {
                    console.error('Errore Stripe:', error);
                    alert('Si è verificato un errore. Riprova.');
                    buttons.forEach(btn => btn.disabled = false);
                    document.getElementById('loading').style.display = 'none';
                }
            } catch (error) {
                console.error('Errore:', error);
                alert('Si è verificato un errore. Riprova.');
                buttons.forEach(btn => btn.disabled = false);
                document.getElementById('loading').style.display = 'none';
            }
        }
    </script>
</body>
</html>